# Auth.js 5 beta still declares Nodemailer 7/8, although the SMTP transport
# used here is compatible with patched Nodemailer 10. Keep clean installs on
# the audited version until Auth.js updates its optional peer range.
legacy-peer-deps=true
